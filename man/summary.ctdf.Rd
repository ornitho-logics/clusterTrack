% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/6_summary.R
\name{summary.ctdf}
\alias{summary.ctdf}
\title{Summarise a ctdf by cluster}
\usage{
\method{summary}{ctdf}(object, ...)
}
\arguments{
\item{object}{A \code{ctdf} (inherits \code{data.table}).}

\item{...}{Currently ignored.}
}
\value{
A \code{summary_ctdf} \code{data.table} .
}
\description{
Returns one row per \code{cluster} with start and stop times, tenure in days,
convex-hull centroid, row count, the 95th percentile of within-cluster \code{lof}
scores, and a cluster elongation score.
}
\details{
\code{lof_q95} is the 95th percentile of lof scores within each cluster.
Larger values indicate that the most extreme points in
the cluster are more locally isolated relative to their neighbours, so this
can be used as a summary of within-cluster outlier-ness.

\code{elongation} is a geometric cluster-shape summary derived from the convex hull
and its minimum rotated rectangle. Larger values indicate clusters that are
both relatively long in one principal dimension and elongated for their area.
}
\examples{
data(mini_ruff)
ctdf = as_ctdf(mini_ruff)
cluster_track(ctdf)
summary(ctdf)


}
\seealso{
\code{\link[=cluster_track]{cluster_track()}}, \code{\link[=ctdf_lof]{ctdf_lof()}}, \code{\link[=ctdf_elongation]{ctdf_elongation()}}
}

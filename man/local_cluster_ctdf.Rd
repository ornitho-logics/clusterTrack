% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/3_cluster.R
\name{local_cluster_ctdf}
\alias{local_cluster_ctdf}
\title{Locally refine putative clusters using DTSCAN}
\usage{
local_cluster_ctdf(ctdf, nmin = 3, area_z_min = 0, length_z_min = 0)
}
\arguments{
\item{ctdf}{A \code{ctdf} object.}

\item{nmin}{Integer; passed as \code{min_pts} to \code{\link[=sf_dtscan]{sf_dtscan()}} when clustering within each
\code{.putative_cluster} region.}

\item{area_z_min}{Numeric; passed to \code{\link[=sf_dtscan]{sf_dtscan()}} as \code{area_z_min}.}

\item{length_z_min}{Numeric; passed to \code{\link[=sf_dtscan]{sf_dtscan()}} as \code{length_z_min}.}
}
\value{
The input \code{ctdf}, invisibly, with \code{.putative_cluster} updated in-place.
}
\description{
Within each existing \code{.putative_cluster} region (typically produced by \code{\link[=slice_ctdf]{slice_ctdf()}}),
run \code{\link[=sf_dtscan]{sf_dtscan()}} on the points in that region to split it into one or more local spatial
subclusters. The resulting labels are combined with the parent \code{.putative_cluster} id and
written back to \code{.putative_cluster} in-place.
}
\seealso{
\code{\link[=sf_dtscan]{sf_dtscan()}}
}

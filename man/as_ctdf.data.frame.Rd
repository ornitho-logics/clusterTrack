% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctdf.R
\name{as_ctdf.data.frame}
\alias{as_ctdf.data.frame}
\title{Coerce an object to clusterTrack data format}
\usage{
\method{as_ctdf}{data.frame}(
  x,
  coords = c("longitude", "latitude"),
  time = "time",
  s_srs = 4326,
  t_srs = "+proj=eqearth",
  ...
)
}
\arguments{
\item{x}{A \code{data.frame} object.}

\item{coords}{Character vector of length 2 specifying the coordinate column names.
Defaults to \code{c("longitude", "latitude")}.}

\item{time}{Name of the POSIXt time column. Will be renamed to \code{"timestamp"} internally.}

\item{s_srs}{Source spatial reference. Default is EPSG:4326}

\item{t_srs}{Target spatial reference passed to \code{\link[sf:st_transform]{sf::st_transform()}}. Default is "+proj=eqearth".}

\item{...}{Currently unused}
}
\value{
An object of class \code{ctdf} (inherits from \code{data.table} and \code{data.frame}),
with an \code{sfc_POINT} geometry column named \code{location}.
}
\description{
Converts an object with spatial coordinates and a timestamp column
to the \code{data.table} format with an \code{sf} geometry column used by clusterTrack.
}
\details{
Rows are sorted by timestamp, geometry is transformed to \code{t_srs}, and
clusterTrack columns are initialized. Existing reserved columns are overwritten
with a warning.

The converted object is checked for duplicate locations with the same timestamp
and temporal gaps exceeding \code{getOption("clusterTrack.max_gap", 24)} hours.
Duplicate warnings identify row positions in the returned, timestamp-sorted \code{ctdf}.
}
\section{ctdf columns}{


In addition to columns retained from the input, a \code{ctdf} contains the
following standardized columns:
\itemize{
\item \code{timestamp}: Observation time. Rows are ordered by this column when the
\code{ctdf} is created.
\item \code{location}: Point geometry in the target coordinate reference system.
\item \code{.id}: Sequential row identifier assigned after ordering by \code{timestamp}.
It is used internally to track locations through the clustering workflow
and does not refer to the row number in the original input.
\item \code{cluster}: Final cluster assignment. It is initialized to \code{NA}.
\code{\link[=cluster_track]{cluster_track()}} assigns positive integers to clustered locations and
\code{0} to unassigned locations. \code{\link[=aggregate_ctdf]{aggregate_ctdf()}} may subsequently merge
and renumber clusters.
\item \code{lof}: Local Outlier Factor score for each location. It is initialized
to \code{NA} and populated by \code{\link[=ctdf_lof]{ctdf_lof()}} for locations with \code{cluster > 0}.
Unassigned locations retain \code{NA}.
}
}

\examples{
data(mini_ruff)
x = as_ctdf(mini_ruff)
plot(x)

}
\seealso{
\code{\link[=as_ctdf]{as_ctdf()}}, \code{\link[=as_ctdf.sf]{as_ctdf.sf()}}
}
